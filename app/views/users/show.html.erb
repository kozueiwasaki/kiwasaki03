<div class="main user-show">
  <div class="container">
    <div class="user">
      <h2><%= @user.name %></h2>
      <p><%= @user.email %></p>
      <img src="<%= "/user_images/#{@user.image_name}" %>">
      <%= link_to "編集", edit_user_path(@user.id) %>
    </div>
  </div>
</div>